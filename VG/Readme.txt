count_lines.py
    It scans the map and if it finds a python file it opens it and counts the lines and then if it finds a dictonary it scans that to.

letter_count.py

pretty_print_subdirectories.py
    It scans every directory and prints it name and then calls the same function again but adds a number to it so it knows how deep it is in the file system.